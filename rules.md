# rules.md
## 文档说明
本文档为本项目AI辅助开发、代码编写、团队协作的强制规范，覆盖项目目录约定、技术栈、Git分支管理、AI协作规则、全端编码规范。所有开发人员、AI生成代码场景均必须遵守，目的是统一代码标准、规避AI幻觉、减少冗余代码、标准化迭代流程，保障项目可维护性。

**核心通用总则：有疑问先问，不要猜。**
所有不确定的需求、技术方案、字段定义、接口规则、数据库结构、历史业务逻辑，必须提前沟通确认，补齐书面依据；禁止主观猜测、自行脑补业务逻辑。

---



### 1.2 项目统一技术栈规范
所有人工开发、AI生成代码严格使用下表技术栈，禁止私自新增框架/依赖、私自升级版本。本项目基于若依多端版本开发，包含后端、Web管理后台、Flutter App。AI生成代码必须遵守若依框架的编码规范、权限体系、返回体封装。

| 技术组件 | 既定版本 | 业务用途说明 |
| ---- | ---- | ---- |
| Flutter | 3.19.6 | Dart自动绑定3.3.4，基于若依App，安卓App客户端 |
| Vue | 3.5.x | Vue3，网页管理后台；使用Vite构建工具，若依Web管理端 |
| Spring Boot | 4.1.0 | Java后端，基于若依(RuoYi-Vue)多端框架开发，支撑Web后台和App两端 |
| JDK | OpenJDK 17 LTS | Java运行环境 |
| Maven | 3.8.8 | 后端项目构建工具 |
| MySQL | 8.0.36 | 项目核心业务数据库，所有业务数据统一存储 |
| mysql-connector-java | 8.0.33 | MySQL JDBC驱动 |

> 后端约束：后端基于若依多端框架开发，需要同时支持Web管理后台、Flutter App两端调用。AI生成后端接口必须遵循若依权限、统一响应封装、Controller/Service/Mapper分层；接口需要考虑两端复用性，禁止硬编码绑定单端逻辑。

### 1.3 Git 分支管理规范

Git分支遵循主干稳定、分支隔离迭代：

- `main`：日常开发分支，功能开发和提交进入这里
- `release`：保护分支，阶段验收通过后由项目经理合并，禁止直接提交

AI产出代码必须人工校验、本地自测后，才允许提交；禁止AI代码在未经人工确认的情况下合并到 `release` 分支。

---

## 二、AI 协作强制核心规范
### 2.1 指定读取范围，禁止全域遍历仓库
使用AI开发、改代码、排查问题时，必须明确告诉AI可读目录、可修改文件、禁止修改的文件。
不允许无限制让AI遍历整个仓库。每次对话前限定文件范围，避免AI误修改若依底层框架代码、配置文件。

### 2.2 先设计后生成，禁止直接编码
不允许AI直接写代码，固定流程：
1. 根据PRD、接口文档、数据库文档输出设计方案，梳理模块拆分、接口设计、两端适配逻辑
2. 人工确认方案
3. 确认方案后，再让AI生成代码

无确认设计方案的AI代码禁止使用。

### 2.3 坚守事实依据，杜绝 AI 幻觉
AI产出的代码、字段、接口、表结构，唯一依据：PRD、接口文档、数据库设计文档。
- 文档不存在的字段、接口路径、表名，AI禁止编造
- 一旦AI虚构字段/接口/表结构，立刻停止使用并纠正
- 无文档依据的功能，先补齐文档再开发

多端场景额外注意：App、Web后台共用后端接口，AI不能自行编造两端各自独立、重复的接口。

### 2.4 出错及时总结，禁止无效重试
当 AI 持续产出错误代码、逻辑偏差、功能不符、反复报错时，禁止无意义重复重试生成，避免浪费资源与开发时间。需立即暂停迭代，复盘问题根源，区分是提示词描述不清、需求信息不全、提供的文档素材有误，还是框架规则适配问题，总结优化后重新发起生成，同时可记录问题沉淀经验，规避后续重复踩坑。

### 2.5 疑问优先确认，杜绝主观猜测
所有 AI 协作开发场景，严格恪守核心底线：有疑问先问，不要猜。需求模糊、字段不确定、接口规则不明、框架逻辑不熟、历史业务逻辑不清等场景，必须先沟通确认、补齐书面依据，禁止人工脑补、禁止 AI 基于猜测生成代码，从源头规避结构性、逻辑性错误。

---

## 三、全端 AI 开发编码规范
### 3.1 后端开发规范
后端核心准则：单一接口原则，一个接口只做一件事。
禁止开发多功能聚合接口，严禁一个接口同时承担新增、查询、修改、删除等多项无关业务操作，禁止接口逻辑臃肿、职责混杂。所有 AI 生成的接口必须职责单一、逻辑纯粹、粒度清晰，完全适配若依框架分层规范、权限校验机制、统一返回格式，便于接口维护、问题排查、前端对接与后续迭代扩展。

### 3.2 前端开发规范
前端核心准则：组件复用原则，同类 UI 统一封装。
Web 管理后台、H5 页面开发，禁止多页面复制粘贴同类 UI、同类交互代码。重复使用的按钮、弹窗、表单、列表、卡片等通用模块，必须统一抽离为公共组件封装至 components 目录。AI 生成前端代码时，优先复用项目已有公共组件，无对应组件则统一封装后再业务使用，保证全站样式统一、交互统一、代码精简。

### 3.3 移动端开发规范
Flutter 客户端开发遵循统一编码规范，适配 3.19.6 版本特性，通用 Widget、公共交互逻辑、全局样式统一抽离封装，杜绝页面级重复代码。AI 生成客户端代码需贴合移动端适配规则、原生交互规范，保证客户端性能与体验统一。


## 四、规范执行与落地说明
1. 本规范为项目强制标准，AI辅助开发、人工开发均需要遵守
2. AI产出的方案、代码、文档，必须人工二次校验，确认符合规范后才能提交
3. 规范可随项目迭代更新，更新后全员同步执行
